import Foundation
import UIKit

/// Service for integrating with a potential cloud-based PanDerm foundation model.
/// NOTE: This service is currently a placeholder to ensure the project compiles.
/// All logic has been removed to prevent conflicts with the local inference service.
class PanDermService: ObservableObject {
    // This class is intentionally left empty for now.
    // It can be used in the future to implement cloud-based API calls
    // without conflicting with the current local inference implementation.
}
